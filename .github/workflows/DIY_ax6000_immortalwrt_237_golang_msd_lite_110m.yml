#=================================================
# Copyright (c) 2019-2023 sky2016cn
# Author: sky2016cn
# licensed under the MIT License
# See /LICENSE for more information
# Description: Build OpenWrt using GitHub Actions
#=================================================

name: DIY_ax6000_immortalwrt_237_golang_msd_lite_110m

# Add a button to trigger the workflow
on: 
  repository_dispatch:
  workflow_dispatch:

  #schedule:
    # UTC 时间对应北京时间（UTC+8=北京时间）：7月20日，早上4点10分 开始编译
    #- cron: '10 20 19 7 *'  
    # UTC 时间对应北京时间：每月2、9、16、23日，早上4点10分 开始编译    
    #- cron: '10 20 1,8,15,22 * *'
    
jobs:
  build:
    # 建议 immortalwrt 使用低版本，更容易编译成功
    runs-on: ubuntu-22.04

    steps:
    - name: Maximize build space
      uses: easimon/maximize-build-space@master
      with:
        root-reserve-mb: 2048
        temp-reserve-mb: 512
        swap-size-mb: 2048
        remove-dotnet: 'true'
        remove-android: 'true'
        remove-haskell: 'true'
        remove-codeql: 'true'
        overprovision-lvm: 'true'
        build-mount-path: '/builder'        

    - name: Prepare Builder Directory
      run: |
        sudo mkdir -p /builder
        sudo chown -R runner:runner /builder
        df -hT

    - name: Checkout repository
      uses: actions/checkout@v7        

    # 如果“Initialization environment”报错，是因为官方 APT 设置依赖项的脚本已经不合时宜，在官方修正之前，请使用下方的代码
    - name: Initialization environment
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo -E apt-get -qq update
        sudo bash -c 'bash <(curl -s https://build-scripts.immortalwrt.org/init_build_environment.sh)'
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo rm -rf /etc/apt/sources.list.d/* /usr/local/lib/android /opt/ghc /usr/share/dotnet /usr/local/graalvm /usr/local/.ghcup \
        /usr/local/share/powershell /usr/local/share/chromium /usr/local/lib/node_modules
        sudo timedatectl set-timezone "Asia/Shanghai"
        sudo chown $USER:$GROUPS $GITHUB_WORKSPACE

    # 使用此代码前，请先更新依赖包的命令：https://github.com/sky2016cn/AutoBuild-lede（依赖包仓库） 
    #- name: Initialization environment
    #  env:
    #    DEBIAN_FRONTEND: noninteractive
    #  run: |  
    #    sudo -E apt-get -qq update
    #    sudo -E apt-get -qq install $(curl -fsSL raw.githubusercontent.com/sky2016cn/OpenWrt-Depends/main/depends-immortalwrt)
    #    sudo -E apt-get -qq autoremove --purge
    #    sudo -E apt-get -qq clean
    #    sudo rm -rf /etc/apt/sources.list.d/* /usr/local/lib/android /opt/ghc /usr/share/dotnet /usr/local/graalvm /usr/local/.ghcup \
    #    /usr/local/share/powershell /usr/local/share/chromium /usr/local/lib/node_modules
    #    sudo timedatectl set-timezone "Asia/Shanghai"
    #    sudo chown $USER:$GROUPS $GITHUB_WORKSPACE
        
    - name: Check Space
      run: |
        df -h

    - name: Clone source code
      env:
        REPO_URL: https://github.com/padavanonly/immortalwrt-mt798x-6.6
      run: |
        # 拉取 237大佬 openwrt-24.10-6.6 分支
        git clone -b openwrt-24.10-6.6 --single-branch --filter=blob:none $REPO_URL /builder/openwrt || { echo 'Git clone failed!'; exit 1; }

       # ========= 添加 immortalwrt 110m 大分区代码 =========
    - name: Apply AX6000 110M Layout Modifications
      working-directory: /builder/openwrt
      run: |
        # 1. 在 filogic.mk 末尾追加 Device（红米AX6000 设备定义）
        cat << 'EOF' >> target/linux/mediatek/image/filogic.mk

        define Device/xiaomi_redmi-router-ax6000-110m
          $(Device/xiaomi_redmi-router-ax6000-stock)
          DEVICE_VARIANT := (hanwckf 110M layout)
          DEVICE_DTS := mt7986a-xiaomi-redmi-router-ax6000-110m
          SUPPORTED_DEVICES := xiaomi,redmi-router-ax6000
          IMAGE_SIZE := 112640k
          KERNEL_IN_UBI := 1
          IMAGES := factory.bin sysupgrade.bin
          IMAGE/factory.bin := append-ubi | check-size $$$$(IMAGE_SIZE)
          IMAGE/sysupgrade.bin := sysupgrade-tar | append-metadata
        endef
        TARGET_DEVICES += xiaomi_redmi-router-ax6000-110m
        EOF

        # 2. 创建全新的 immortalwrt 110m大分区 DTS 设备树文件
        cat << 'EOF' > target/linux/mediatek/dts/mt7986a-xiaomi-redmi-router-ax6000-110m.dts
        // SPDX-License-Identifier: (GPL-2.0 OR MIT)

        /dts-v1/;
        #include "mt7986a-xiaomi-redmi-router-ax6000.dtsi"

        / {
        	model = "Xiaomi Redmi Router AX6000 (hanwckf 110M layout)";
        	compatible = "xiaomi,redmi-router-ax6000", "mediatek,mt7986a";
        };

        &spi_nand_flash {
        	mediatek,nmbm;
        	mediatek,bmt-max-ratio = <1>;
        	mediatek,bmt-max-reserved-blocks = <64>;
        };

        &partitions {
        	partition@580000 {
        		label = "crash";
        		reg = <0x580000 0x40000>;
        		read-only;
        	};

        	partition@5c0000 {
        		label = "crash_log";
        		reg = <0x5c0000 0x40000>;
        		read-only;
        	};

        	partition@600000 {
        		label = "ubi";
        		reg = <0x600000 0x6e00000>;
        	};
        };
        EOF

        echo "✅ AX6000 110M 大分区适配代码补丁应用成功！"
        
    - name: Update feeds（更新 feeds）
      working-directory: /builder/openwrt
      run: |
        ./scripts/feeds update -a        

       #======= 解决 Passwall 的 Geoview 报错问题（置于更新和安装 feeds 命令之间）=======
    - name: 升级 Passwall 工具链为 Golang 1.26
      working-directory: /builder/openwrt
      run: |
        # 1. 直接将本地仓库根目录下的 golang1.26 复制到 feeds 目录
        rm -rf feeds/packages/lang/golang1.26        
        mkdir -p feeds/packages/lang/golang1.26
        cp -rf "$GITHUB_WORKSPACE/golang1.26/." feeds/packages/lang/golang1.26/

        # 2. 强制更新索引并安装 golang1.26（同时刷入 feeds 索引列表）
        ./scripts/feeds update -f packages
        ./scripts/feeds install -p packages -f golang1.26
        
        # 3. 删除临时目录
        rm -rf tmp
        
    - name: Install feeds（安装 feeds）
      working-directory: /builder/openwrt
      run: |
        ./scripts/feeds install -a

    - name: Install feeds（安装 feeds）
      working-directory: /builder/openwrt
      run: |
        ./scripts/feeds install -a
        
       #======= 编入第三方 msd_lite 插件，解决 IPTV 卡顿（更新和安装 feeds 命令之后）=======
    - name: 编入第三方 msd_lite 插件
      working-directory: /builder/openwrt
      run: |
        rm -rf feeds/packages/net/msd_lite
        rm -rf feeds/luci/applications/luci-app-msd_lite
        git clone https://github.com/ximiTech/msd_lite.git package/msd_lite
        git clone https://github.com/ximiTech/luci-app-msd_lite.git package/luci-app-msd_lite
        
        # 删除临时目录
        rm -rf tmp
               
       # ======== 1. 编译 OpenClash 最新版，使用 package 编译法（更新和安装 feeds 命令之后）推荐 =========
       # 不推荐使用写入 ./feeds.conf.default 编译法
       # immortalwrt 插件库已经包含 OpenClash，若源码被删除或编译 OpenClash 最新版，使用以下命令自行编译，打开 immortalwrt官方插件库查询：https://github.com/immortalwrt/immortalwrt（选择 openwrt-24.10分支，打开 feeds.conf.default文件，再打开luci仓库找到“applications”查询插件）
    #- name: Import Official Recommended OpenClash
    #  working-directory: /builder/openwrt
    #  run: |
        # 移除 openwrt feeds 自带的核心库，
    #    rm -rf package/luci-app-openclash
    #    rm -rf feeds/luci/applications/luci-app-openclash

        # 克隆 OpenClash 最新版
    #    git clone --depth=1 https://github.com/vernesong/OpenClash.git /tmp/OpenClash

        # 只移动 luci-app-openclash 软件包到 package 目录下
    #    mv /tmp/OpenClash/luci-app-openclash package/

        # 删除临时目录
    #    rm -rf /tmp/OpenClash && rm -rf tmp

       # ======== 2. 编译 Passwall 最新版，使用 package 编译法（更新和安装 feeds 命令之后）推荐 =========
       # 不推荐使用写入 ./feeds.conf.default 编译法
       # immortalwrt 插件库已经包含 OpenClash，若源码被删除或编译 Passwall 最新版，使用以下命令自行编译，打开 immortalwrt官方插件库查询：https://github.com/immortalwrt/immortalwrt（选择 openwrt-24.10分支，打开 feeds.conf.default文件，再打开luci仓库找到“applications”查询插件）
    - name: Import Official Recommended Passwall
      working-directory: /builder/openwrt
      run: |
        # 移除 openwrt feeds 自带的核心库
        rm -rf feeds/packages/net/{xray-core,v2ray-geodata,sing-box,chinadns-ng,dns2socks,hysteria,ipt2socks,microsocks,naiveproxy,shadowsocks-rust,shadowsocksr-libev,simple-obfs,tcping,v2ray-plugin,xray-plugin,geoview,shadow-tls}
        git clone https://github.com/Openwrt-Passwall/openwrt-passwall-packages package/passwall-packages

        # 移除 openwrt feeds 过时的luci版本
        rm -rf feeds/luci/applications/luci-app-passwall
        git clone https://github.com/Openwrt-Passwall/openwrt-passwall package/passwall-luci

        # 删除临时目录
        rm -rf tmp

    - name: 修改 passwall-packages 目录下所有 Makefile 文件中 golang 路径指向 golang1.26
      working-directory: /builder/openwrt
      run: |
        # 1. 修改 passwall-packages 目录下所有 Makefile 文件中 golang 路径指向 golang1.26
        find package/passwall-packages -type f -name "Makefile" \
          -exec sed -i \
            -e 's|\<golang/golang-package.mk\>|golang1.26/golang-package.mk|g' \
            -e 's|\<golang/host\>|golang1.26/host|g' {} +
        
        # 2. 检查是否成功替换 Golang1.26 版本，如果退出，说明 golang 版本与 Passwall 要求的 golang 版本对不上。通过以下方法确认版本已经不一致，请打开 https://github.com/Openwrt-Passwall/openwrt-passwall-packages，打开 geoview/Makefile 文件，搜索“golang”并获取相关路径：/feeds/packages/lang/golang/golang-package.mk，确认能在本地仓库 golang1.26 目录下找到“golang-package.mk”文件，且路径一致，否则就是版本对不上（运行脚本将 golang1.26 目录复制并改名为 golang，所以路径等于文件中的 /feeds/packages/lang/golang 目录）
        echo "=== 检查是否成功替换 Golang1.26 版本 ==="
        grep -E "PKG_VERSION:=" feeds/packages/lang/golang1.26/golang1.26/Makefile || {
          echo "ERROR: golang1.26 替换失败！"
          exit 1
        }
        
        # 3. 检查修改结果（抽查核心组件 xray-core 和 sing-box），检查失败会自动退出
        echo "=== 检查 package/passwall-packages 下 所有的 Makefile 文件中的 Go 编译依赖指向 golang1.26 ==="
        grep -n "golang1.26" package/passwall-packages/xray-core/Makefile && \
        grep -n "golang1.26" package/passwall-packages/sing-box/Makefile || {
          echo "ERROR: Passwall Packages Makefile 文件中路径错误替换失败！"
          exit 1
        }
        
        # 删除临时目录
        rm -rf tmp        
        
    - name: Configuration Customization - DIY_ax6000_immortalwrt_237_golang_msd_lite_110m 
      run: |
        # 在升级的分支，找到“Quickstart”下方：5. 看到 "# MT7986" 配置文件命令：cp -f defconfig/mt7986-ax6000.config .config 可确认该分支包含 红米AX6000官方原厂闭源
        # 将配置文件从 defconfig 目录复制到根目录并重命名 .config，云编译的目录和官方目录不同，所以命令中必须修改路径
        cp -f /builder/openwrt/defconfig/mt7986-ax6000.config /builder/openwrt/.config
        
        # 追加你要的插件配置，打开 feeds.conf.default文件，再打开luci仓库，选择“openwrt-24.10”分支，找到“applications”查询插件
        cat >> /builder/openwrt/.config <<EOF
        CONFIG_TARGET_mediatek=y
        CONFIG_TARGET_mediatek_filogic=y
        CONFIG_TARGET_mediatek_filogic_DEVICE_xiaomi_redmi-router-ax6000-110m=y
        CONFIG_PACKAGE_luci-app-autoreboot=y
        CONFIG_PACKAGE_luci-app-msd_lite=y
        CONFIG_PACKAGE_luci-app-openclash=y
        # ================= Passwall 主程序 =================
        CONFIG_PACKAGE_luci-app-passwall=y
        # 用于读取和解析大陆 IP/域名数据库（GeoIP / GeoSite）的轻量级工具
        CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Geoview=y

        # ================= 核心代理内核 (Sing-Box + Xray) =================
        CONFIG_PACKAGE_luci-app-passwall_INCLUDE_SingBox=y
        CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Xray=y

        # ================= 显式关闭不必要的组件与插件 =================
        # 负载均衡
        # CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Haproxy is not set
        # Shadowsocks 客户端
        # CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Shadowsocks_Rust_Client is not set
        # Shadowsocks 服务端
        # CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Shadowsocks_Rust_Server is not set
        # ShadowsocksR 客户端
        # CONFIG_PACKAGE_luci-app-passwall_INCLUDE_ShadowsocksR_Libev_Client is not set
        # ShadowsocksR 服务端
        # CONFIG_PACKAGE_luci-app-passwall_INCLUDE_ShadowsocksR_Libev_Server is not set
        # Hysteria v1 独立客户端
        # CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Hysteria is not set
        # NaiveProxy 独立客户端
        # CONFIG_PACKAGE_luci-app-passwall_INCLUDE_NaiveProxy is not set
        # Shadow_TLS 独立客户端
        # CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Shadow_TLS is not set
        # Simple_Obfs 老旧混淆插件 (Shadowsocks 混淆插件，给 SS 做混淆)
        # CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Simple_Obfs is not set
        # V2ray 独立客户端
        # CONFIG_PACKAGE_luci-app-passwall_INCLUDE_V2ray_Plugin is not set
        # Xray-Plugin 混淆插件 (Shadowsocks 混淆插件，给 SS 做混淆，非 Xray 核心)
        # CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Xray_Plugin is not set
        # V2ray_Geodata 旧版数据包
        # CONFIG_PACKAGE_luci-app-passwall_INCLUDE_V2ray_Geodata is not set
        EOF
        
        # Modify default IP 将默认IP地址 192.168.6.1 修改为 192.168.2.1
        sed -i 's/192.168.6.1/192.168.2.1/g' /builder/openwrt/package/base-files/files/bin/config_generate
        cd /builder/openwrt && rm -rf tmp && make defconfig

    - name: Download package
      working-directory: /builder/openwrt
      run: |
        make download -j$(nproc)
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    - name: Build firmware
      working-directory: /builder/openwrt
      # 编译超过 4 小时，自动退出编译，防止后台静默卡死
      timeout-minutes: 240
      env:
        # 禁用 Git 终端交互提示，遇到需要输入密码或确认时，自动退出编译，防止后台静默卡死
        GIT_TERMINAL_PROMPT: 0
        # 禁用 Git 隐式文件锁，避免在多进程/并发 Git 操作时因锁争用导致的异常
        GIT_OPTIONAL_LOCKS: 0
      run: |
        echo -e "$(nproc) thread build."
        # 先尝试多线程全速编译，如果失败（返回非 0 状态码），则触发单线程编译，重新编译并输出详细日志
        make -j$(nproc) || make -j1 V=s

    - name: Check Space
      run: |
        df -h

    - name: Organize Files (整理固件与插件)
      id: organize
      working-directory: /builder/openwrt
      if: success() && !cancelled()
      run: |
        # 1. 创建专门存放固件的临时目录
        mkdir -p /tmp/firmware

        # 2. 定位 targets 编译产物目录（在绝对路径 /builder/openwrt/bin/targets/ 下寻找）
        TARGET_DIR=$(find /builder/openwrt/bin/targets/ -maxdepth 2 -mindepth 2 -type d | head -n 1)
        
        if [ -d "$TARGET_DIR" ]; then
          # 复制所有 targets 镜像、itb 文件、manifest、bl2.bin、sha256sums 等
          cp -r $TARGET_DIR/* /tmp/firmware/ 2>/dev/null || true
        fi

        # 3. 复制并重命名配置文件
        cp /builder/openwrt/.config /tmp/firmware/config.buildinfo 2>/dev/null || true
        cp /builder/openwrt/.config /tmp/firmware/build.config 2>/dev/null || true

        # 4. 打包所有编译生成的 ipk/apk 插件为 Packages.tar.gz
        if [ -d "/builder/openwrt/bin/packages" ]; then
          mkdir -p /tmp/pkg_temp
          find /builder/openwrt/bin/packages -type f -name "*.ipk" -exec cp -f {} /tmp/pkg_temp/ \;
          tar -zcf /tmp/firmware/Packages.tar.gz -C /tmp pkg_temp
          rm -rf /tmp/pkg_temp
        fi

        # 5. 精准清理不需要的临时 Packages 文件夹和多余的中间 buildinfo
        rm -rf /tmp/firmware/packages /tmp/firmware/feeds.buildinfo /tmp/firmware/version.buildinfo 2>/dev/null || true

        # ===============================================================
        # 👇【修改区】修改成脚本的名称
        FIRMWARE_TYPE="DIY_ax6000_immortalwrt_237_golang_msd_lite_110m"
        # ===============================================================

        # 6. 将 FIRMWARE_TYPE 连同 TAG 和 NAME 一起写入 GITHUB_ENV 传递给后续步骤
        echo "FIRMWARE_TYPE=${FIRMWARE_TYPE}" >> $GITHUB_ENV
        echo "RELEASE_TAG=${FIRMWARE_TYPE}_$(date +'%Y.%m.%d-%H%M')" >> $GITHUB_ENV
        echo "RELEASE_NAME=${FIRMWARE_TYPE} [$(date +'%Y.%m.%d')]" >> $GITHUB_ENV

    - name: Upload firmware to Release (发布至 GitHub Release)
      uses: softprops/action-gh-release@v2
      if: success() && !cancelled()
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: '${{ env.RELEASE_TAG }}'
        name: '${{ env.RELEASE_NAME }}'
        body: |
          ### 固件信息
          ${{ env.FIRMWARE_TYPE }}
        files: /tmp/firmware/*
